[pytest]
testpaths =
    test
    input_guardrails/tests
    output_guardrails
    rag/experiments
markers =
    integration: mark test as integration (needs raw_data/)
    slow: mark test as slow (needs heavy models)
